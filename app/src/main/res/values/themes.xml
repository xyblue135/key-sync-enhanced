<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.KeySync" parent="android:Theme.Material.NoActionBar" />

    <style name="Theme.KeySync.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.KeySync</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    </style>
</resources>